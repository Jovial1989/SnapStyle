// Single source of truth for the store links. Referenced by every CTA
// (hero, closing band, header). Swap these two lines the moment the real
// listings are issued — nothing else needs to change.
//
// [NEED: real store URLs] — placeholders until the listings go live (days away).
export const APP_STORE_URL = "#app-store-coming"; // App Store listing
export const GOOGLE_PLAY_URL = "#google-play-coming"; // Google Play listing

// Flip to true once BOTH listings are live to drop any "Soon" affordance.
export const APP_STORE_LIVE = false;
export const GOOGLE_PLAY_LIVE = false;
